name: GitHub Actions - Push - Branch Ignore Template
run-name: Running - ${{ github.event_name }}
on:
  push:
    paths:
      - main
jobs:
  on-push:

    runs-on: ubuntu-latest
    steps:
      - name: Comentário
        run: |
          echo "Comentário PUSH WORKFLOW - Branch Ignore"

