name: Github Actions - Pull Request
run-name: Running - ${{ github.event_name }}
on: [workflow_dispatch]
jobs:
  on-workflow_dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: comentário
        run: |
          echo "PULL REQUEST WORKFLOW - Action"