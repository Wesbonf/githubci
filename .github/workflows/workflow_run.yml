name: Github Actions - Condicionais
run-name: Running - ${{ github.event_name }} 
on: [push, workflow_dispatch]

jobs:
  if: 
  on-push-only:
    runs-on: ubuntu-latest
    steps:
      - name: Comentário
        run: echo ${{ github.event_name }}


  on_workflow-dispatch-only:
    if: 
    runs-on: ubuntu-latest
    steps:
      - name: Comentário
        run: |
          echo ${{ github}}