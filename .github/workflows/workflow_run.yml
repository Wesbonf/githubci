name: Github Actions - Configuração de variável de repositório
run-name: Configurando variável de repositório
on:
  push:
    branches:
      - main

jobs:
  on-push:
    runs-on: ubuntu-latest
    steps:
      - name: Comentário
        run: |
          echo "${{ vars.var_teste}}"